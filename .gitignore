# ── Built outputs ─────────────────────────────────────────────────────────
*.apk
*.aar
*.ap_
*.aab
*.dex
*.class

# ── Generated / build dirs ────────────────────────────────────────────────
bin/
gen/
out/
build/
.gradle/

# ── Gradle wrapper binary (auto-downloaded; not committed) ────────────────
gradle/wrapper/gradle-wrapper.jar

# ── Local SDK / IDE config ────────────────────────────────────────────────
local.properties
*.iml

# ── IntelliJ / Android Studio ────────────────────────────────────────────
.idea/workspace.xml
.idea/tasks.xml
.idea/gradle.xml
.idea/assetWizardSettings.xml
.idea/dictionaries/
.idea/libraries/
.idea/caches/
.idea/modules.xml
.idea/*.xml
.idea/navEditor.xml
captures/
.navigation/

# ── Room schema snapshots (optionally committed for migration tracking) ────
# app/schemas/

# ── Keystore files ────────────────────────────────────────────────────────
# Uncomment if signing files should not be tracked
#*.jks
#*.keystore
# Always exclude release keystores
release.jks
release.keystore

# ── Native build artefacts ────────────────────────────────────────────────
.externalNativeBuild/
.cxx/

# ── Logs / profiling ──────────────────────────────────────────────────────
*.log
*.hprof

# ── macOS ─────────────────────────────────────────────────────────────────
.DS_Store

# ── Windows ───────────────────────────────────────────────────────────────
Thumbs.db

# ── Scratch/debug artifacts (may contain leaked credentials) ──────────────
C:\\Users\\bobot\\AppData\\Local\\Temp\\adb_scratch\\phone_logcat.txt

# ── Local agent tooling (per-machine state, never shared) ─────────────────
.codex/
.tokensave/
